import { Button } from "@/components/ui/button";
import React from "react";

const page = () => {
  return (
    <div>
      <Button className="cursor-pointer m-10">Click</Button>
    </div>
  );
};

export default page;
